{
  "name": "member-panel",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/member-panel",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/member-panel",
        "outputPath": "dist/apps/member-panel"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/member-panel"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "member-panel:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "member-panel:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "member-panel:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "member-panel:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/member-panel/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/member-panel/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc --pretty --noEmit --project apps/member-panel/tsconfig.json",
          "tsc --pretty --noEmit --project apps/member-panel/tsconfig.spec.json"
        ]
      }
    }
  },
  "tags": []
}
